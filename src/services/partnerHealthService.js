// Adapter boundary for a future NSFDC/official partner health feed.
// Returning null intentionally means the UI falls back to only published mock signals.
export const getOfficialFeed = async () => null;
